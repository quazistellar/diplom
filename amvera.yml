meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  command: gunicorn unireax.wsgi:application --bind 0.0.0.0:8000
  persistenceMount: /data
  containerPort: 8000